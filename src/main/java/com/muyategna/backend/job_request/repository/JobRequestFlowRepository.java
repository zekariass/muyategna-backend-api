package com.muyategna.backend.job_request.repository;

import com.muyategna.backend.job_request.entity.JobRequestFlow;
import org.springframework.data.jpa.repository.JpaRepository;

public interface JobRequestFlowRepository extends JpaRepository<JobRequestFlow, Long> {
}
